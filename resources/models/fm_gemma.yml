resources:
  model_serving_endpoints:
    gemma_endpoint:
      name: 'gemma-12b-it'
      config:
        served_entities:
          - entity_name: 'system.ai.gemma-3-12b-it'
            name: 'gemma-12b-it'
            min_provisioned_throughput: 0
            max_provisioned_throughput: 5000
            scale_to_zero_enabled: true
        traffic_config:
          routes:
            - served_model_name: 'gemma-12b-it'
              traffic_percentage: 100
